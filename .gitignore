# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local environments
.venv/
venv/

# Local app state and generated outputs
.streamlit/secrets.toml
outputs/
runs/
tmp/
*.log

# Pretrained weights auto-downloaded by Ultralytics into the CWD
yolo*.pt
yolo*.yaml

# Trained model artifacts (keep metadata/configs in Git)
models/*.engine
models/*.torchscript
models/detector/
models/classifier/
training/artifacts/

# Ultralytics writes its own settings here when run from the repo root:
# machine-specific dataset/weights paths plus a per-install uuid, so it is
# local tool state, not project source. The folder is left where the tool
# expects it; only Git ignores it.
Ultralytics/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Legacy prototype: RTSP credentials and a 130 MB binary, kept on disk for
# reference but never in Git.
legacy/rtsp.txt
legacy/KCS_Inspec_PCBA_V2.exe
